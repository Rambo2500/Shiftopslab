import React from 'react';
import { 
  BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, LineChart, Line 
} from 'recharts';
import { 
  Activity, Package, Truck, AlertTriangle, CheckCircle, BarChart3, Clock, Database 
} from 'lucide-react';

const data = [
  { name: 'Mon', value: 400 },
  { name: 'Tue', value: 300 },
  { name: 'Wed', value: 200 },
  { name: 'Thu', value: 278 },
  { name: 'Fri', value: 189 },
];

const StatCard = ({ title, value, icon: Icon, color }) => (
  <div className="bg-slate-900 border border-slate-800 p-6 rounded-xl shadow-lg">
    <div className="flex items-center justify-between">
      <div>
        <p className="text-slate-400 text-xs font-semibold uppercase tracking-wider">{title}</p>
        <h3 className="text-2xl font-bold mt-1 text-white">{value}</h3>
      </div>
      <div className={`p-3 rounded-lg ${color} bg-opacity-10 text-${color.split('-')[1]}-500`}>
        <Icon size={24} />
      </div>
    </div>
  </div>
);

function Dashboard() {
  return (
    <div className="min-h-screen bg-slate-950 text-slate-50 p-8 font-sans">
      {/* Header */}
      <div className="flex items-center justify-between mb-8 border-b border-slate-800 pb-6">
        <div>
          <h1 className="text-3xl font-extrabold tracking-tight text-white">{{ goal }}</h1>
          <p className="text-slate-400 mt-1">Autonomous Systems Management Console</p>
        </div>
        <div className="flex gap-4">
          <div className="flex items-center gap-2 bg-blue-500/10 text-blue-400 px-4 py-2 rounded-full border border-blue-500/20 text-sm font-medium">
            <Activity size={16} /> Live Data
          </div>
          <div className="bg-slate-900 border border-slate-800 px-4 py-2 rounded-lg text-sm">
            {new Date().toLocaleDateString()}
          </div>
        </div>
      </div>

      {/* Stats Grid */}
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
        <StatCard title="Active Ingestion" value="1,284 / hr" icon={Database} color="bg-blue-500" />
        <StatCard title="System Throughput" value="94.2%" icon={Activity} color="bg-emerald-500" />
        <StatCard title="Active Carriers" value="42" icon={Truck} color="bg-amber-500" />
        <StatCard title="Process Alerts" value="3" icon={AlertTriangle} color="bg-rose-500" />
      </div>

      {/* Charts Section */}
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">
        {/* Performance Chart */}
        <div className="bg-slate-900 border border-slate-800 p-6 rounded-xl shadow-lg">
          <div className="flex items-center justify-between mb-6">
            <h3 className="font-bold flex items-center gap-2">
              <BarChart3 size={20} className="text-blue-500" /> System Performance
            </h3>
            <select className="bg-slate-950 border border-slate-800 text-xs rounded px-2 py-1 outline-none">
              <option>Last 7 Days</option>
              <option>Last 30 Days</option>
            </select>
          </div>
          <div className="h-64">
            <ResponsiveContainer width="100%" height="100%">
              <BarChart data={data}>
                <CartesianGrid strokeDasharray="3 3" stroke="#1e293b" vertical={false} />
                <XAxis dataKey="name" stroke="#64748b" fontSize={12} tickLine={false} axisLine={false} />
                <YAxis stroke="#64748b" fontSize={12} tickLine={false} axisLine={false} />
                <Tooltip 
                  contentStyle={% raw %}{{ backgroundColor: '#0f172a', border: '1px solid #1e293b', borderRadius: '8px' }}{% endraw %}
                  itemStyle={% raw %}{{ color: '#f8fafc' }}{% endraw %}
                />
                <Bar dataKey="value" fill="#3b82f6" radius={[4, 4, 0, 0]} barSize={30} />
              </BarChart>
            </ResponsiveContainer>
          </div>
        </div>

        {/* Resilience Analytics */}
        <div className="bg-slate-900 border border-slate-800 p-6 rounded-xl shadow-lg">
          <div className="flex items-center justify-between mb-6">
            <h3 className="font-bold flex items-center gap-2">
              <Clock size={20} className="text-emerald-500" /> Resilience Latency
            </h3>
            <button className="text-xs text-blue-400 hover:text-blue-300 font-medium">Download Report</button>
          </div>
          <div className="h-64">
            <ResponsiveContainer width="100%" height="100%">
              <LineChart data={data}>
                <CartesianGrid strokeDasharray="3 3" stroke="#1e293b" vertical={false} />
                <XAxis dataKey="name" stroke="#64748b" fontSize={12} tickLine={false} axisLine={false} />
                <YAxis stroke="#64748b" fontSize={12} tickLine={false} axisLine={false} />
                <Tooltip 
                  contentStyle={% raw %}{{ backgroundColor: '#0f172a', border: '1px solid #1e293b', borderRadius: '8px' }}{% endraw %}
                  itemStyle={% raw %}{{ color: '#f8fafc' }}{% endraw %}
                />
                <Line type="monotone" dataKey="value" stroke="#10b981" strokeWidth={3} dot={% raw %}{{ r: 4, fill: '#10b981' }}{% endraw %} />
              </LineChart>
            </ResponsiveContainer>
          </div>
        </div>
      </div>

      {/* Components Footer */}
      <div className="mt-8 bg-slate-900/50 border border-slate-800/50 p-4 rounded-lg flex items-center justify-between text-xs text-slate-500">
        <div className="flex gap-6">
          <span>Engine: ShiftOps-OS v1.0</span>
          <span>Architecture: Validated DAG</span>
          <span>Domain: Logistics / Operations</span>
        </div>
        <div className="flex items-center gap-2">
          <CheckCircle size={14} className="text-emerald-500" /> All systems operational
        </div>
      </div>
    </div>
  );
}

export default Dashboard;

